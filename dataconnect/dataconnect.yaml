specVersion: "v1beta"
serviceId: "peerplace"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "peerplace-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
